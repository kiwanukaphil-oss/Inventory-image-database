# Copy this file to ".env" and point the PWA at the Railway POS API. Browser
# clients receive only this public API origin; database and bucket credentials
# stay in Railway service variables.

VITE_CATALOG_API_URL=http://localhost:5000/api
