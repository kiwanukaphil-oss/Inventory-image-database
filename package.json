{
  "name": "inventory-image-database",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mobile-first catalog PWA backed by the Railway POS API and private object storage.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:db": "node scripts/verify-migrations.mjs",
    "verify:railway-only": "node scripts/verify-railway-only.mjs",
    "test:watch": "vitest",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "seed": "node --env-file=.env scripts/seed.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "5.3.0",
    "@supabase/supabase-js": "2.112.4"
  },
  "devDependencies": {
    "exceljs": "4.4.0",
    "sharp": "0.35.3",
    "vite": "8.1.4",
    "vite-plugin-pwa": "1.3.0",
    "vitest": "4.1.10"
  },
  "overrides": {
    "uuid": "11.1.1"
  }
}
