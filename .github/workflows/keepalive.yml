name: Retired database keepalive

# CANDIDATE FOR REMOVAL: retained for one review phase as migration evidence.
# It has no schedule, credentials, network request, or runnable job.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "Legacy keepalive retired; Railway services use platform health checks."
